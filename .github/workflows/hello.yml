name: Hello Workflow

on:
  push:

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print greeting
        run: echo "Hello from CI"

      - name: Print date
        run: date

      - name: Print commit hash
        run: git rev-parse HEAD
